// const dotenv = require('dotenv');

// dotenv.config();

module.exports = {
    // parser: 'sugarss',
    map: 'false',
    plugins: {
      'postcss-import': {},
      'postcss-preset-env': {},
      'cssnano': {}
    }
  }